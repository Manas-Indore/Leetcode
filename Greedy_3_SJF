import java.util.*;

class Solution {
    public long solve(int[] bt) {

        Arrays.sort(bt);

        long waiting = 0;
        long total = 0;

        for (int i = 0; i < bt.length; i++) {
            total += waiting;
            waiting += bt[i];
        }

        return total / bt.length;
    }
}
